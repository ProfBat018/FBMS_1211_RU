import React from 'react';
import {Layout} from "./Layout";
import Auth from "./Auth";

const Home = () => {
    return (
        <div>
            <Auth/>
        </div>
    );
};

export default Home;