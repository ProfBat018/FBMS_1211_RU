<UserControl x:Class="MVVM_Views.View.ThirdView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVM_Views.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="450"
             Background="White">
    <Grid>
        <StackPanel>
            <Button Height="100" FontSize="50" Content="Back" Command="{Binding BackCommand}"/>
            <Label Height="100" FontSize="50" Content="{Binding Person.Name}" />
            <Label Height="100" FontSize="50" Content="{Binding Person.Surname}" />
            <Label Height="100" FontSize="50" Content="{Binding Person.Age}" />
        </StackPanel>
    </Grid>
</UserControl>
